# recordings & transcripts (large / private - never commit)
meetings/
recordings/
*.wav
*.flac
*.mp3
*.txt
*.srt
*.vtt

# ...except the tiny real-audio e2e fixture clip (committed on purpose)
!tests/fixtures/**

# models
models/
*.bin
*.gguf

# local secrets (HF_TOKEN etc.) - never commit
mise.local.toml

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
